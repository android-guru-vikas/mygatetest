<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="bigTextViewStyle">
        <item name="android:textColor">@android:color/black</item>
        <item name="android:padding">@dimen/eight_dp</item>
        <item name="android:textSize">@dimen/eighteen_sp</item>

        <item name="android:layout_marginLeft">@dimen/eighteen_dp</item>
        <item name="android:layout_marginStart">@dimen/eighteen_dp</item>
        <item name="android:layout_marginRight">@dimen/eighteen_dp</item>
        <item name="android:layout_marginEnd">@dimen/eighteen_dp</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:shadowRadius">2</item>

    </style>

    <style name="smallTextViewStyle">
        <item name="android:textColor">@android:color/black</item>
        <item name="android:padding">@dimen/eight_dp</item>
        <item name="android:textSize">@dimen/fourteen_sp</item>

        <item name="android:layout_marginLeft">@dimen/eighteen_dp</item>
        <item name="android:layout_marginStart">@dimen/eighteen_dp</item>
        <item name="android:layout_marginRight">@dimen/eighteen_dp</item>
        <item name="android:layout_marginEnd">@dimen/eighteen_dp</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:shadowRadius">2</item>

    </style>
</resources>
